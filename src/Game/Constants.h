#ifndef CONSTANTS_H
#define CONSTANTS_H

#include <qglobal.h>

const int WINDOW_WIDTH = 400;
const int WINDOW_HEIGHT = 600;

const qreal BIRD_X_POSITION = 50.0;
const qreal BIRD_SIZE = 20.0;
const qreal GRAVITY = 0.5;

const qreal FLAP_STRENGTH = -10.0;

const int PIPE_WIDTH = 50;
const int PIPE_GAP = 150;
const int PIPE_SPACING = 220;
const int PIPE_SPEED = 3;

const int GAME_TIMER_INTERVAL = 16;

#endif
